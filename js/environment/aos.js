        // アニメーションの初期設定
        AOS.init({
            // アニメーションの持続時間（ミリ秒）
            duration: 1000,
            // スクロールのオフセット値（ピクセル）
            offset: 200,
            // アニメーションを1回だけ実行
            once: true
        });